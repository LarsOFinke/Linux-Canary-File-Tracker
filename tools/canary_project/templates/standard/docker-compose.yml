services:
  web:
    build: .
    ports:
      - "8080:8080"
    environment:
      APP_ENV: production
      APP_SECRET: canary-secret
      DATABASE_URL: postgres://app:secret@db:5432/app
    volumes:
      - .:/app
